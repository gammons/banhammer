package me.grantammons.rogueEngine.tests;

import org.testng.annotations.Test;

public class NoticedEntitiesTest {
    @Test(description = "noticing an enemy")
    public void testNoticingAnEmeny() {
        // somethingIsNoticed could potentially add to the noticed list
        // clear + repopulate noticed list

    }
}
